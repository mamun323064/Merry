
$(function () {
    /*--------------------------
	 Global Variable
	---------------------------- */
    var patsala = $(window);
    var page = $('html, body');

    $(window).on('load', function () {
        $('.preloader').delay(1500).fadeOut(500);

    });

    //     $('.venobox').venobox();

});
